package proxy:recorder@0.1.0;

interface %record {
  %record: func(method: string, args: list<string>, ret: string);
}

interface replay {
  replay: func(method: string, args: option<string>, is-export: bool) -> string;
}

world host {
  import %record;
}

